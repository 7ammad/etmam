/**
 * Temporary demo route: Framer Motion (KPI + list stagger) and Lottie.
 * Visit /[locale]/demo-motion (e.g. /en/demo-motion or /ar/demo-motion).
 * Respects prefers-reduced-motion (WCAG 2.2).
 */

import { DemoMotionPageClient } from '@/components/demo/demo-motion-page-client'

export default function DemoMotionPage() {
  return <DemoMotionPageClient />
}
